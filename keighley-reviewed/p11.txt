if(!renderer)return;camera.aspect=innerWidth/innerHeight;camera.fov=innerWidth<760?50:34;camera.updateProjectionMatrix();renderer.setPixelRatio(Math.min(devicePixelRatio,innerWidth<760?1.05:1.5));renderer.setSize(innerWidth,innerHeight);const wrap=$('#viewfinder-wrap');const r=wrap.getBoundingClientRect();if(viewRenderer&&r.width&&r.height){viewRenderer.setSize(r.width,r.height,false);viewCamera.aspect=r.width/r.height;viewCamera.updateProjectionMatrix()}}
function onScroll(){const max=document.documentElement.scrollHeight-innerHeight;const p=max?scrollY/max:0;$('#film-progress').style.height=`${p*100}%`;const secs=$$('.section');const focusY=innerHeight*.48;let best=0,bd=1e9;secs.forEach((s,i)=>{const r=s.getBoundingClientRect();const d=Math.abs((r.top+r.bottom)*.5-focusY);if(d<bd){bd=d;best=i}});activeSection=Math.min(best,7);secs.forEach((s,i)=>s.classList.toggle('active',i===activeSection));$('#section-count').textContent=`${String(activeSection+1).padStart(2,'0')} / 08`;$('#exploded-labels').classList.toggle('visible',activeSection===2);$('#leader-svg').classList.toggle('visible',activeSection===2&&!mobile);$('#finale-ui').classList.toggle('visible',activeSection===6);if(activeSection!==2)setHovered(null);if(activeSection===5)animateCounts();}
let counted=false;function animateCounts(){if(counted)return;counted=true;const start=performance.now();const run=now=>{const t=clamp((now-start)/1500);$$('[data-count]').forEach(el=>{const n=+el.dataset.count;const v=Math.floor(n*(1-Math.pow(1-t,3)));el.textContent=n>999?v.toLocaleString()+'+':v+(n===6?'+':'')});if(t<1)requestAnimationFrame(run)};requestAnimationFrame(run)}
function sectionProgress(i){const s=$(`.section[data-index="${i}"]`);if(!s)return 0;const r=s.getBoundingClientRect();return clamp((innerHeight-r.top)/(innerHeight+r.height));}
function setExplode(t){t=reduced?(activeSection===2?1:0):smooth(t);for(const id of ['body','lens','magazine','lcd','viewfinder']){const o=parts[id],a=assembled[id].p,target=a.clone().add(explodedTargets[id]);o.position.lerpVectors(a,target,t);if(id==='lens')o.rotation.y=lerp(0,-.16,t);if(id==='magazine')o.rotation.z=lerp(0,.13,t);if(id==='viewfinder')o.rotation.z=lerp(0,-.11,t)}}
function updateLeaders(){if(activeSection!==2)return;const ids=['body','lens','magazine','lcd','viewfinder'];for(const id of ids){const obj=parts[id];const v=new THREE.Vector3();obj.getWorldPosition(v);v.project(camera);const x=(v.x*.5+.5)*innerWidth,y=(-v.y*.5+.5)*innerHeight;const label=$(`.part-label[data-id="${id}"]`).getBoundingClientRect();const tx=label.left<label.width?label.right:label.left;const ty=label.top+label.height*.5;const path=$(`#line-${id}`),dot=$(`#dot-${id}`);path.setAttribute('d',`M${x},${y} C${lerp(x,tx,.5)},${y} ${lerp(x,tx,.5)},${ty} ${tx},${ty}`);dot.setAttribute('cx',x);dot.setAttribute('cy',y)}}
function animate(){requestAnimationFrame(animate);if(hidden)return;const dt=Math.min(clock.getDelta(),.033),t=clock.elapsedTime,mt=reduced?0:t;
 const p0=sectionProgress(0),p1=sectionProgress(1),p2=sectionProgress(2),p3=sectionProgress(3),p4=sectionProgress(4),p5=sectionProgress(5),p6=sectionProgress(6),p7=sectionProgress(7);
 // Reset visibility/state
 rig.visible=activeSection<7;odyssey.visible=activeSection>=6;filmAssembly.visible=activeSection===1||activeSection===2;blimp.visible=activeSection===3;periscope.visible=activeSection===4;
 if(activeSection<=1){setExplode(0);rig.position.set(0,.15,0);rig.rotation.set(lerp(.03,-.04,p0),lerp(-.22,.22,p0)+mt*.035,0);rig.scale.setScalar((mobile?.78:1)*lerp(.72,.64,p1));camera.position.lerp(shot(7.4,3.4,12.2),ease(.05));camera.lookAt(0,.05,0);postFocus.value=7.2;}
 if(activeSection===1){rig.rotation.set(0,.02,0);camera.position.lerp(shot(6.6,2.3,11.8),ease(.06));for(let i=0;i<filmAssembly.children.length;i++){const o=filmAssembly.children[i];if(o.material===MAT.film||o.material===MAT.black)o.position.x+=dt*1.15;if(o.position.x>1.8)o.position.x-=3.38}postFocus.value=6.4;}
 if(activeSection===2){const e=clamp((p2-.18)/.62);setExplode(e);rig.position.set(0,.1,0);rig.rotation.y=lerp(.05,-.12,e);rig.scale.setScalar(innerWidth<760?.52:.61);camera.position.lerp(shot(7.2,2.4,13.5),ease(.08));camera.lookAt(0,.1,0);postFocus.value=7.4;updateLeaders();}
 if(activeSection===3){setExplode(0);rig.rotation.set(0,0,0);rig.scale.setScalar(.5);rig.position.set(0,lerp(2.4,-.18,reduced?1:smooth(clamp((p3-.18)/.56))),0);blimp.position.set(0,-.15,0);blimp.rotation.y=-.02;camera.position.lerp(shot(8.2,3.1,13.8),ease(.06));camera.lookAt(0,-.05,0);postFocus.value=8.4;}
 if(activeSection===4){setExplode(0);rig.scale.setScalar(.65);rig.position.set(0,.1,0);rig.rotation.y=.08;camera.position.lerp(shot(6.2,2.7,11.2),ease(.08));camera.lookAt(-.6,.4,0);periscope.scale.setScalar(reduced?.75:.75*smooth(clamp((p4-.18)