ow.position.set(-8,8,-18);g.add(moonGlow);
 // stars
 const pts=[];for(let i=0;i<700;i++){const a=Math.random()*Math.PI*2;const r=25+Math.random()*35;pts.push(Math.cos(a)*r,8+Math.random()*28,-10-Math.random()*45)}const pg=new THREE.BufferGeometry();pg.setAttribute('position',new THREE.Float32BufferAttribute(pts,3));g.add(new THREE.Points(pg,new THREE.PointsMaterial({color:0xd8e6ff,size:.04,sizeAttenuation:true,transparent:true,opacity:.82})));
 // ship
 const ship=new THREE.Group();ship.position.set(4,-1.75,-16);ship.rotation.y=-.2;g.add(ship);const hullShape=new THREE.Shape();hullShape.moveTo(-4.2,.8);hullShape.quadraticCurveTo(-3.7,-1.1,0,-1.35);hullShape.quadraticCurveTo(3.8,-1.05,4.5,.72);hullShape.lineTo(3.3,.28);hullShape.quadraticCurveTo(0,-.35,-3.4,.25);hullShape.closePath();const hullGeo=new THREE.ExtrudeGeometry(hullShape,{depth:1.7,bevelEnabled:true,bevelSegments:4,steps:1,bevelSize:.12,bevelThickness:.12});hullGeo.center();const hull=new THREE.Mesh(hullGeo,new THREE.MeshStandardMaterial({color:0x342014,roughness:.76}));hull.rotation.y=Math.PI/2;hull.castShadow=true;ship.add(hull);const deck=rounded(7.1,.18,1.5,.08,new THREE.MeshStandardMaterial({color:0x6a4127,roughness:.72}),5);deck.position.y=.66;ship.add(deck);
 const mast=cyl(.1,.12,8,new THREE.MeshStandardMaterial({color:0x4c2f1c,roughness:.8}),20);mast.rotation.z=0;mast.position.y=4.35;ship.add(mast);const sailGeo=new THREE.PlaneGeometry(4.6,5.3,8,8);const sp=sailGeo.attributes.position;for(let i=0;i<sp.count;i++){const x=sp.getX(i),y=sp.getY(i);sp.setZ(i,Math.sin((x/4.6+.5)*Math.PI)*.42+Math.sin(y*.7)*.04)}sp.needsUpdate=true;sailGeo.computeVertexNormals();const sail=new THREE.Mesh(sailGeo,new THREE.MeshStandardMaterial({color:0xc3b38c,roughness:.84,side:THREE.DoubleSide}));sail.position.set(-1.7,4.45,0);sail.castShadow=true;ship.add(sail);const yard=cyl(.08,.08,5.1,new THREE.MeshStandardMaterial({color:0x4c2f1c,roughness:.8}),16);yard.rotation.z=.07;yard.position.set(-1.25,6.7,.05);ship.add(yard);
 for(let i=0;i<12;i++){const oar=cyl(.045,.065,3.6,new THREE.MeshStandardMaterial({color:0x765036,roughness:.8}),12);oar.rotation.z=Math.PI/2+.42;oar.position.set(-3+i*.55,.05,i%2?1.45:-1.45);oar.rotation.x=i%2?.15:-.15;ship.add(oar)}
 // foreground rocks for rack focus
 const rockMat=new THREE.MeshStandardMaterial({color:0x17191c,roughness:.93});for(let i=0;i<8;i++){const r=new THREE.Mesh(new THREE.DodecahedronGeometry(.7+Math.random()*1.3,1),rockMat);r.scale.set(1.6,.7,1);r.position.set(-7+i*2.2,-2.35,-2-Math.random()*4);r.rotation.set(Math.random(),Math.random(),Math.random());r.castShadow=true;g.add(r)}
 const fogPlane=new THREE.Mesh(new THREE.PlaneGeometry(70,18),new THREE.MeshBasicMaterial({color:0x263752,transparent:true,opacity:.075,depthWrite:false}));fogPlane.position.set(0,1,-22);g.add(fogPlane);return g}

async function init(){
 clock=new THREE.Clock();scene=new THREE.Scene();scene.background=new THREE.Color(0x050505);scene.fog=new THREE.FogExp2(0x08090b,.009);
 camera=new THREE.PerspectiveCamera(mobile?50:34,innerWidth/innerHeight,.05,180);camera.position.copy(shot(7.8,3.8,12));camera.lookAt(0,0,0);
 renderer=new THREE.WebGPURenderer({antialias:!lowPower,powerPreference:'high-performance',forceWebGL:lowPower||!navigator.gpu});renderer.setPixelRatio(Math.min(devicePixelRatio,mobile?1.05:1.5));renderer.setSize(innerWidth,innerHeight);renderer.shadowMap.enabled=true;renderer.shadowMap.type=THREE.PCFSoftShadowMap;renderer.toneMapping=THREE.ACESFilmicToneMapping;renderer.toneMappingExposure=1.22;renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.domElement.id='main-canvas';stage.prepend(renderer.domElement);await renderer.init();
 statusEl.textContent=navigator.gpu?'WebGPU · ACES · TSL':'WebGL2 backend · ACES · TSL';
 const pmrem=new THREE.PMREMGenerator(renderer);scene.environment=pmrem.fromScene(new RoomEnvironment(),.04).texture;pmrem.dispose();
 const floor=new THREE.Mesh(new THREE.PlaneGeometry(70,70),new THREE.MeshPhysicalMaterial({color:0x080808,metalness:.38,roughness:.31}));floor.rotation.x=-Math.PI/2;floor.position.y=-2.15;floor.receiveShadow=true;scene.add(floor);
 const key=new THREE.SpotLight(0xffe2bd,620,45,.62,.75,1.3);key.position.set(-8,10,8);key.target.position.set(0,0,0);key.castShadow=true;key.shadow.mapSize.set(2048,2048);key.shadow.bias=-.00012;scene.add(key,key.target);
 const fill=new THREE.RectAreaLight(0x9eb8ff,35,8,8);fill.position.set(9,2,6);fill.lookAt(0,0,0);scene.add(fill);
 const rim=new THREE.SpotLight(0xe8a33d,480,35,.48,.8,1.5);rim.position.set(4,7,-9);rim.target.position.set(0,0,0);rim.castShadow=true;scene.add(rim,rim.target);
 scene.add(new THREE.AmbientLight(0x5a6680,.28));
 rig=createCameraRig();scene.add(rig);blimp=createBlimp();scene.add(blimp);periscope=createPeriscope();rig.add(periscope);od